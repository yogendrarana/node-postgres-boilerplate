import { expect, describe, it } from "vitest"

describe("user", () => {
    describe("user test", () => {
        describe("POST /user", () => {
            it("should return 200", async () => {
                expect(1).toBe(1)
            })
        })
    })
})